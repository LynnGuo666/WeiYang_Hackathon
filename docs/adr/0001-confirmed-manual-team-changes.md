---
status: accepted
---

# 手动组队变更必须经成员确认后写入

选手卡片发起的添加和删除都先创建有效期 15 分钟的待确认变更，只有被操作成员确认后才写入队伍关系；队伍表在确认前保持不变，且同一队伍同一时间只允许一个待确认变更。确认时必须在锁内重新读取并校验最新队伍和成员状态，以保证一人一队并处理并发邀请。这样可以避免队长单方面改变成员关系，同时允许队伍暂时少于 3 人或缺少大一新生而不自动解散；后续添加再按“缺少大一新生时必须优先添加大一新生”的策略校验。
