# 未央黑客松 飞书选手服务机器人

读取「活动报名」多维表格，维护一个选手数据库（一人一个 ID），选手私聊机器人发送「验证」即可通过飞书账号手机号完成授权，验证通过后自动拉入所有配置的交流群；支持定时自动同步与补拉。

## 架构

```
bot/
  main.py            入口：WebSocket 长连接 + APScheduler 定时任务
  core/
    config.py        .env 配置
    permissions.py   管理员身份目录与权限查询
    lark_client.py   飞书 API 封装（发消息/读手机号/拉群）
    base_store.py    多维表格读写（cli / sdk 双后端）
    registry.py      插件注册器（含 public/user/admin 作用域）
    event_bus.py     事件路由与统一权限拦截
    card_kit.py      卡片模板
  modules/           功能插件（按管理员/用户侧显式装配）
    admin/           管理员指令与后台任务装配
    user/            用户侧指令、卡片回调与后台钩子装配
    sync/            报名表 -> 选手库 同步（手机号去重、队友拆分、队伍关系）
    auth/            验证授权（读飞书手机号 -> 比对选手库 -> 绑定 open_id）
    group/           拉群 / 补拉（群配置表驱动、拉群记录留痕、群成员实时数据幂等）
    profile/         个人中心（选手 ID / 队伍 / 项目提交状态）
    score/           积分（骨架）
    activity/        活跃度（骨架）
    ticket/          票务（骨架）
scripts/
  init_base.py       全量同步脚本
  backfill.py        手动补拉脚本
  describe_fields.py 给 Base 所有表字段写描述注释（幂等；Link 字段 API 不支持，见脚本内文档）
```

扩展新功能：领域模块只实现业务服务和 handler，在 `modules/user/__init__.py` 或
`modules/admin/commands.py`、`modules/admin/jobs.py` 中显式装配。用户指令使用
`REGISTRY.user_command(...)`，管理员指令使用 `REGISTRY.admin_command(...)`；权限由
`core.event_bus` 统一拦截，不要在业务 handler 中重复调用 `is_admin`。

## 选手数据库（Base `QD2ubyLMraQlNrsA3eicyrwhngh`）

| 表 | 用途 |
|---|---|
| 选手表 | 一人一条记录，`选手ID`（WY01-0001）唯一；手机号为去重键；`发言数`/`积分` 由机器人自动累计 |
| 组委会表 | 组委会名单：姓名/手机号（去重键）/身份（导师、主办方，可增删）/绑定码/飞书open_id/验证状态。导师若同时以选手报名，用「绑定 <码>」额外绑定 |
| 队伍表 | 仅「是否预组队=是」（明确组队成功）的报名记录入表，队长/队友 link 到选手表 |
| 群配置表 | 机器人要拉的群清单（群名 / chat_id / 启用 / **面向身份**多选：选手、组委会-导师、组委会-主办方、全部；留空视为选手） |
| 拉群记录表 | 每次拉群留痕（审计；是否已在群里以飞书群成员实时数据为准） |
| 项目表 | 参赛项目（项目ID/名称/介绍/队伍/成员/仓库与演示链接/票数） |
| 投票表 | 决赛投票流水（投票人 → 项目），一人一票由机器人校验 |
| 积分表 / 活跃度表 | 扩展功能流水表（活跃度表由群发言监控自动写入）；积分总分冗余在选手表「积分」字段 |

去重规则：手机号归一化后必须通过大陆手机号格式校验（`1[3-9]` 开头 11 位），无效手机号直接跳过不同步；同一人出现在主报名人和多个队伍的队友栏时只保留一个选手 ID，审核状态取最优（任一队伍通过即通过）。

## 快速开始

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env          # 填入 FEISHU_APP_ID / FEISHU_APP_SECRET
.venv/bin/python scripts/init_base.py   # 全量同步（也可跳过，机器人启动后会自动同步）
.venv/bin/python -m bot.main            # 启动机器人
```

## 飞书开发者后台配置清单

1. 创建企业自建应用，启用「机器人」能力。
2. 开通权限：`im:message:send_as_bot`、`im:message:readonly`、`im:chat:create`、`im:chat:read`、`im:chat:update`、`im:chat:moderation:write_only`、`im:chat.members:write_only`、`im:chat.members:read`、`bitable:app`、`contact:user.base:readonly`、**获取用户手机号**（`contact:user.employee_id:readonly` 所属的手机号读取权限）、`application:application:self_manage`（管理员识别，或 `admin:app.info:readonly`）。
3. 事件与回调：订阅方式选择「**使用长连接接收事件**」，订阅 `im.message.receive_v1`；如需卡片交互，开启回调配置。
4. 把「活动报名」Base 和「选手数据库」Base 添加应用为**协作者**（可编辑）。
5. 应用可用范围加入需要验证的选手。
6. 把应用的 App ID / App Secret 填入 `.env`。

## 机器人指令

### 用户侧

| 指令 | 说明 |
|---|---|
| `验证` / `授权` / `验证身份` | 私聊机器人，读取飞书账号手机号（外部用户填表单：手机号+vx号）与选手库比对，绑定 open_id。**验证=绑定身份，与审核解耦**：审核未通过也会绑定成功，审核通过后自动拉群。选手表未命中时自动转**组委会验证**（手机号+姓名双因子），通过后按身份拉入组委会群 |
| `绑定 <码>` | 已验证用户（如以选手身份报名的导师）用组委会**绑定码**额外绑定组委会身份，拉入对应组委会群；选手身份不受影响 |
| `个人中心` / `我的` | 我的选手 ID、验证/审核状态、队伍与队友、项目提交状态（帮助卡也有「个人中心」按钮） |
| `查分` / `积分` | 查看我的积分总分与最近流水（总分冗余在选手表「积分」字段） |
| `活跃` / `活跃度` | 查看群发言活跃度排行（机器人自动监控群发言累计） |
| `投票` | 决赛投票：机器人回复项目列表卡片，点按钮投票，一人一票。**票数不公开**：项目列表和投票成功提示都不显示票数 |
| `票数` / `票榜` | 查看当前票榜（只显示排名不显示票数；管理员可见票数） |
| `帮助` / `help` / `菜单` | 查看帮助卡片和快捷操作。私聊发送任意未知消息也会打开帮助卡片 |

### 管理员侧

| 指令 | 说明 |
|---|---|
| `生成绑定码` | 给组委会表里绑定码为空的成员批量生成随机绑定码（ZZB-XXXXXXXX，去易混淆字符），并私聊回执分发清单 |
| `开票` / `关票` | 开关投票通道 |
| `同步` | 手动触发一次报名表同步 |
| `重载数据` | 手动修改 Base 后，强制全量刷新机器人内存数据 |
| `补拉` | 把所有已验证用户补进与其身份匹配的缺失群（以群成员实时数据为准，新增群自动覆盖） |
| `建群 <群名> [面向身份]` | 机器人建**外部群**（可拉外部成员），发指令的管理员当群主，机器人自动设为群管理员，自动登记进群配置表（面向身份默认选手，可逗号分隔多个） |
| `管理员` | 查看当前管理员名单与来源 |

其他任意消息回复帮助卡片（含快捷按钮：验证 / 投票 / 活跃度 / 票榜，点按钮即触发）。

**所有指令均仅在私聊触发**：在群里 @机器人 发指令不会被响应（静默忽略，不私聊用户；群发言活跃度统计照常）。群里回复帮助卡片也仅限私聊场景。

## 卡片

全部使用**新版卡片 JSON 2.0**（`schema: "2.0"`）：帮助卡片带快捷操作按钮（behaviors callback），验证表单用 form 容器 + 提交按钮（`form_action_type: "submit"`），投票卡片每个项目一个回调按钮。回调事件仍为 `card.action.trigger`，按钮值在 `action.value`、表单值在 `action.form_value`。要求飞书客户端 7.20+（低版本只显示标题）。

## 管理员识别

管理员 = **飞书开发者后台的应用协作者**（owner / administrator 等）。`bot/core/permissions.py`
在启动时调「获取应用协作者」API 拉取名单，之后每 10 分钟自动刷新；需要权限
`application:application:self_manage` 或 `admin:app.info:readonly`（任一）。`.env` 的
`ADMIN_OPEN_IDS` 作为兜底（API 失败时仍可用）。

## 验证与审核的关系

- **验证**：确认「这个飞书账号 = 报名表里的某个人」，成功即绑定 open_id、标记已验证。
- **审核状态**：只由报名表同步刷新（审核状态取最优：任一队伍通过即通过）。**在报名表里改审核状态后，下一次同步（定时 10 分钟或「同步」指令）会自动覆盖选手表**——选手表里手改无效，报名表才是唯一事实来源。
- **拉群**：验证成功且审核通过 → 立即拉群；验证成功但未审核 → 等审核通过后由「审核通过补拉」定时任务（每 30 分钟）自动拉群，无需重新验证。

## 活跃度监控

机器人在群内收到每条消息（含非文本）时，若发言者是已验证选手，则选手表「发言数」+1 并在活跃度表写一条「发言」流水。`活跃` 指令查看 Top 10 排行，管理员额外可见总发言数。

## 群配置

在「选手数据库 → 群配置表」添加行：群名、`chat_id`（oc_ 开头，可让机器人进群后通过 API 查询）、勾选「启用」。验证通过的用户会被拉入所有启用中的群。

外部群必须由用户担任群主，但建群接口会同时把创建群的机器人设为群管理员，后续机器人才能自动拉人。旧群若仍报 `232017`，请由群主在「群设置 → 群管理」中将机器人设为管理员；若客户端无法设置机器人管理员，则把「谁可以添加群成员、分享群」改为「所有群成员」。

如果旧群是机器人创建的，也可以在开发者后台额外开通 `im:chat:operate_as_owner` 并发布应用，让机器人按创建者权限操作；该方式不适用于由其他用户创建的群。

**群限制**：机器人建群后会自动套用旧选手群的限制模板：仅群主/管理员可拉人、分享群、@所有人、编辑群信息、加急和发起视频会议；需要审批入群；所有成员可发言；入群提示对所有成员可见，退群提示仅群主/管理员可见，成员人数对所有成员可见。飞书当前没有可靠的官方服务端接口修改「置顶管理权限」，该项需在客户端手动设置。更新已有群时可使用 `bot.core.lark_client` 中的通知群、选手群和组委会群模板。

**新增群怎么办**：不需要任何手动操作。机器人默认每 30 分钟（`BACKFILL_INTERVAL_MINUTES`，设 0 禁用）自动补拉一次——按群成员实时数据找出还没进新群的已验证用户并拉入；也可以随时让管理员发「补拉」立即执行。是否已在群里完全以飞书群成员列表为准，不依赖任何本地状态字段。

**按身份分流**：群配置表「面向身份」多选决定该群拉哪些人——`选手`（默认）、`组委会-导师`、`组委会-主办方`、`全部`。组委会表「身份」列新增选项后，群配置表的选项会自动同步（每次同步任务检查）。组委会成员验证通过后按其身份拉入匹配的群；双重身份（选手+组委会）两边匹配的群都会拉。
